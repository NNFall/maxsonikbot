# План миграции Telegram -> MAX

Дата старта: 2026-04-06  
Текущий статус: `Этап 5 (в работе)`

## Этапы

### Этап 0. Аудит и доступ к MAX — завершен
- Проверена структура проекта и совместимость архитектуры.
- Подтверждено подключение к MAX через тестовый echo-скрипт.

### Этап 1. Каркас MAX-бота — завершен
- Перенесен entrypoint на `maxapi` (`main.py`).
- Добавлены `max_handlers` и `max_keyboards`.
- Адаптирован `notify_admin` под MAX (`chat_id`/`user_id`).

### Этап 2. Пользовательский Tarot flow — завершен
- Перенесены сценарии `start`/`menu`/`help`/`invite`.
- Перенесен сценарий `ask -> teaser/full`.
- Перенесен follow-up контекст по раскладу.

### Этап 3. Монетизация и подписки — завершен
- Перенесен `balance/subscription` flow.
- Перенесен цикл оплаты YooKassa (create/poll/activate/recurrent).
- Перенесены `pending_actions` (автопродолжение после оплаты).
- Подключен `subscription_watcher`.

### Этап 4. Админка, рассылка, deeplink — завершен
- Подключен admin-router и команды управления.
- Добавлена MAX smart-рассылка (`services/smart_mailer_max.py`).
- Проверены deeplink payload-ветки: `ref_`, `promo_`, `utm`.

### Этап 5. Финализация, деплой и эксплуатация — в работе
- Деплой выполнен в `/root/maxtarobot` через Docker.
- БД вынесена в host-volume (`/root/maxtarobot/data/database.db`).
- Идет эксплуатационная доводка и smoke-проверки.

## Журнал выполнения

### 2026-04-06
- Добавлен `tools/max_echo_test.py`, проверен токен MAX.
- Перенесены базовые роутеры и клавиатуры MAX.
- Перенесены tarot/payments/subscriptions сценарии.
- Исправлен баг `NameError: crud is not defined` в `max_handlers/start.py`.

### 2026-04-07
- Восстановлена установка команд через `set_my_commands`.
- Подтверждено применение команд к боту в MAX.

### 2026-04-08
- Выполнен деплой на сервер `185.171.83.116` в `/root/maxtarobot`.
- Исправлен конфликт зависимостей для Docker-сборки (`aiofiles`).
- Подтвержден внешний mount БД: `/root/maxtarobot/data/database.db`.
- Найдена и устранена причина дублей сообщений (несколько инстансов с одним токеном).
- Добавлен админ `5462936` в `.env`.
- Добавлена команда `/admin_help`.

### 2026-04-09 — 2026-04-12
- Удален legacy-код Telegram из рабочей структуры проекта.
- Обновлен продакшен после чистки.
- Добавлен и настроен flow прогресса генерации:
  - стикер ожидания;
  - одно процессовое сообщение;
  - удаление обоих после генерации.
- Добавлено сообщение об успешной оплате перед автопродолжением полного расклада.

## Риски и ограничения
- В MAX API нет полноценного аналога Telegram command scope для UI-разделения команд.
- `set_my_commands` в `maxapi` работает, но помечен библиотекой как deprecated относительно официальной swagger MAX.
- Запуск нескольких инстансов с одним токеном приводит к дублированию ответов.

## Что осталось до полного закрытия
- Прогнать финальный smoke:
  - `/start` -> `/ask` -> trial -> paywall -> YooKassa -> полный расклад.
- Проверить админ-команды в живом чате:
  - `/admin_help`, `/botstats`, `/genpromo`, `/adstats_all`.
- Финально зафиксировать pre-release checklist и runbook в операционном формате.
